import 'package:flutter/material.dart';

class Talk2Statue extends StatelessWidget {
  const Talk2Statue({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}